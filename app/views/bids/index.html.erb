<%- model_class = Bid -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:review_request_id) %></th>
      <th><%= model_class.human_attribute_name(:bidder_id) %></th>
      <th><%= model_class.human_attribute_name(:bid_amount) %></th>
      <th><%= model_class.human_attribute_name(:bid_message) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @bids.each do |bid| %>
      <tr>
        <td><%= link_to bid.id, bid_path(bid) %></td>
        <td><%= bid.review_request_id %></td>
        <td><%= bid.bidder_id %></td>
        <td><%= bid.bid_amount %></td>
        <td><%= bid.bid_message %></td>
        <td><%=l bid.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_bid_path(bid), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      bid_path(bid),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_bid_path,
            :class => 'btn btn-primary' %>
